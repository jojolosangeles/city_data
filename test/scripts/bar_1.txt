# Load collision data into "bar_graph_1" DataFrame
load data/sample_20_Traffic_Collision_Data_from_2010_to_Present.csv as bar_graph_1

# Display the columns loaded (to verify the load was successful)
columns bar_graph_1

# Get rid of the columns we aren't using
drop bar_graph_1 DR Number, Date Reported, Area ID, Reporting District
drop bar_graph_1 Crime Code, Crime Code Description, MO Codes, Premise Code
drop bar_graph_1 Premise Description, Location

# Create some new columns
create bar_graph_1 Date [pd.to_datetime(x) for x in dataFrame['Date Occurred']]
create bar_graph_1 Weekday [x.weekday() for x in dataFrame['Date']]
create bar_graph_1 Year [x.year for x in dataFrame['Date']]

# Show the current state of the columns (to verify changes)
columns bar_graph_1

# Save the DataFrame "bar_graph_1" into a CSV file "bar_graph_1.csv"
save bar_graph_1

# Draw a bar graph using "Year" for X axis, and "Number of Accidents" for Y axis
# Notation for axes is passed to 
bar bar_graph_1 X|Year:O|Year Y|count()|Number+of+Accidents
